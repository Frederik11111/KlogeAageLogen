#include "simulate.h"
#include <stdio.h>

struct Stat simulate(struct memory *mem, int start_addr, FILE *log_file, struct symbols* symbols) {
    // This is just a placeholder so the code compiles.
    // We will implement the actual logic in Phase 3.
    
    printf("Simulator has not been implemented yet!\n");
    
    struct Stat stats;
    stats.insns = 0;
    return stats;
}